info:
  name: Chat Detail
  type: http
  seq: 2
http:
  method: GET
  url: '{{base_url}}/mobile/chats/1'
  body:
    type: none
  auth: inherit
docs: |
  Bir chatning to'liq detali — list summary + pinlangan **candidate_card** snapshot + **service stages** tracker (faqat ariza qabul qilingan bo'lsa).

  **Auth:** `access_token`.

  ### 📋 Headers

  | Header | Required | Description |
  |---|:---:|---|
  | `Authorization` | ✅ | `Bearer {access_token}` |

  ### 📤 Response

  List item DTO + qo'shimcha:

  | Field | Type | Description |
  |---|---|---|
  | `candidate_card` | object \| null | Pinlangan obyektivka. `{candidate_id, application_id, full_name, photo_url, phone, cv_id, cv_title, cover_message}`. Conversation ochilishda system message sifatida birinchi xabar sifatida |
  | `service_groups` | array | **Xizmatlar tashkilot bo'yicha guruhlangan** + guruh bo'yicha ketma-ket to'lov holati (bosqichlar guruh ichida `stages[]` da). Bo'sh massiv — pipeline yo'q. (Quyida tuzilishi) |
  | `offer` | object \| null | **Taklif + funding holati**. `null` — tashkilot arizani hali qabul qilmagan (pipeline yo'q). Nomzod taklifni shu yerdan **qabul/rad/bekor** qiladi: `POST /mobile/chats/<id>/offer/{accept,decline,fund,cancel}` |
  | `active_call` | object \| null | **Aktiv (davom etayotgan/jiringlayotgan) qo'ng'iroq**. `null` — qo'shiladigan qo'ng'iroq yo'q. Push kelmagan / chat qayta ochilgan holatda mijoz shu maydon orqali "Join" tugmasini ko'rsatadi va `POST /mobile/chats/<id>/video-call/join` chaqiradi. Obyekt: `{call_message_id, room_name, domain, started_by, started_at}` |

  `offer` obyekti (mavjud bo'lsa):

  | Field | Type | Description |
  |---|---|---|
  | `id` | int | Pipeline (taklif) id |
  | `pipeline_outcome` | string | `active` \| `completed` \| `cancelled` |
  | `current_stage` | string | `awaiting` (qabul kutilmoqda) / `training` / … / `completed` / `cancelled` |
  | `confirmed` | bool | Taklif qabul qilinganmi (pul band qilish boshlanganmi) |
  | `currency` | string | Band qilish valyutasi (UZS) |
  | `required_amount` | int | Barcha xizmatlar jami (so'm) |
  | `funded_amount` | int | Band qilingan (boshlangan) summa |
  | `awaiting_amount` | int | **Boshlangan** (awaiting_payment) — hozir to'lash mumkin bo'lgan summa |
  | `pending_amount` | int | Hali boshlanmagan (pending) guruhlar summasi — hali to'lab bo'lmaydi |
  | `reserved_amount` | int | Jami band qilingan |
  | `captured_amount` | int | Xizmatlarga yechib o'tkazilgan |
  | `remaining_hold` | int | Hali band'da turgan (bekor qilishda qaytadigan) |

  > `offer` faqat **jami** ko'rsatkichlar. Per-xizmat detal va tashkilot bo'yicha guruhlangan to'lov holati yuqori darajadagi `service_groups[]` (va undagi `stages[]`) da.

  ### 🗂 `service_groups[]` — tashkilot bo'yicha guruh

  Oqim: **Start → Pay → Execute**. Admin/tashkilot guruhni boshlaydi (`started`), shunda kandidat to'lay oladi. To'lov **guruh bo'yicha, ketma-ket** (sort tartibida; oldingi to'lanmaguncha keyingisi `is_current` bo'lmaydi). Guruh ichida — hammasi yoki hech narsa.

  > **Kurs chegirmasi (Task 21):** agar nomzod vakansiyaning `training` bosqichidagi kursni **avval to'liq sotib olgan** bo'lsa (mustaqil mobil xarid yoki boshqa vakansiya doirasida), o'sha bosqich avtomatik **bepul** bo'ladi: `amount = 0`, `discount_amount = original_amount`. Chegirma bosqich yaratilganda (taklif qabulida) bir marta hisoblanadi.

  | Field | Type | Description |
  |---|---|---|
  | `service_group_id` | int \| null | Guruhning `VacancyServiceGroup.id`'si — `POST /admin/chats/<id>/groups/{groupId}/start` uchun ishlatiladi. Eski/guruhlanmagan bosqichlar uchun `null` |
  | `organization` | object \| null | `{id, name, type, type_label, logo_url}` — xizmatni beruvchi tashkilot |
  | `responsible_user` | object \| null | Tashkilotning mas'ul useri: `{id, username, full_name, phone, photo_url}` |
  | `stages` | array | Shu guruhdagi bosqichlar (quyidagi `stages[]` struktura) |
  | `required_amount` | int | Guruh jami — **chegirmadan keyingi** to'lanadigan summa (so'm) |
  | `funded_amount` | int | Guruhda band qilingan (so'm) |
  | `awaiting_amount` | int | Guruhni to'lash uchun yetishmayotgan (so'm) |
  | `discount_amount` | int | **Guruhdagi jami chegirma** (so'm) — avval to'liq sotib olingan kurslar bepul bo'lganda > 0 |
  | `currency` | string | Valyuta (UZS) |
  | `started` | bool | Admin/tashkilot guruhni boshlaganmi (to'lov oynasi ochilganmi) |
  | `awaiting_start` | bool | Guruh hali boshlanmagan (kandidat boshlanishini kutmoqda) |
  | `fully_funded` | bool | Guruhning barcha (skipped emas) bosqichlari to'langanmi |
  | `is_current` | bool | **Hozir to'lanishi mumkin** (boshlangan + to'lanmagan). Ketma-ket vakansiyada faqat birinchi shunday guruh; `single_payment` da barcha boshlangan to'lanmagan guruhlar |
  | `payment_deadline_at` | int \| null | Guruhning to'lov muddati (unix ts; boshlangach o'rnatiladi) |
  | `paid_percent` | int | **Guruh uchun necha foiz to'langani** (`funded_amount / required_amount`, 0–100) |
  | `progress_percent` | int | Guruh o'rtacha bajarilish progressi (0–100) |

  Har `service_groups[].stages[]` elementi:

  | Field | Type | Description |
  |---|---|---|
  | `id` | int | stage id |
  | `service_group_id` | int \| null | tegishli kategoriya guruhi id |
  | `category` | string | `agency` \| `notarius` \| `insurance` \| `logistics` \| `visa` \| `translation_apostille` \| `training` |
  | `category_label` | string | tarjima qilingan label |
  | `service_name` | string \| null | vacancy_service snapshot nomi |
  | `amount` | int | **To'lanadigan summa** (so'm) — qabul paytida snapshot. Avval to'liq sotib olingan kurs uchun `0` (chegirma) |
  | `discount_amount` | int | **Chegirma** (so'm). `training` bosqichida, nomzod o'sha kursni avval to'liq sotib olgan bo'lsa = `original_amount` (bosqich bepul); aks holda `0` |
  | `original_amount` | int | Chegirmagacha bo'lgan asl narx (so'm) = `amount + discount_amount` |
  | `currency` | string | Valyuta (UZS) |
  | `funded` | bool | Nomzod puli shu xizmat uchun band qilinganmi (to'langanmi) |
  | `status` | string | `pending` \| `awaiting_payment` \| `in_progress` \| `completed` \| `overdue` \| `payment_overdue` \| `skipped` |
  | `status_label` | string | tarjima qilingan |
  | `responsible_user` | object \| null | Tashkilotdan: `{id, username, full_name, phone, photo_url}` (mas'ul — xizmatni beruvchi tashkilot useri) |
  | `organization` | object \| null | `{id, name, type, type_label, logo_url}` — xizmatni beruvchi tashkilot |
  | `started_at` | int \| null | Guruh boshlangan vaqt (unix ts) |
  | `payment_deadline_at` | int \| null | To'lov muddati (unix ts) — `awaiting_payment` da |
  | `payment_time_left_text` | string \| null | To'lovgacha qolgan vaqt (server-localized) |
  | `deadline_at` | int \| null | Ijro muddati (unix ts) — to'langan kundan |
  | `deadline_at_formatted` | string \| null | `d.m.Y H:i` (Asia/Tashkent) |
  | `completed_at` | int \| null | |
  | `completed_at_formatted` | string \| null | `d.m.Y H:i` (Asia/Tashkent) |
  | `progress_percent` | int | 0–100 |
  | `time_left_text` | string \| null | "7 d", "1 d, 2 h", "Deadline passed" — server-localized |
  | `sort_order` | int | UI tartibi |

  ### ⚠️ Errors

  | Code | Reason |
  |---|---|
  | `401` | Token yo'q |
  | `403` | Chat boshqa nomzodga tegishli |
  | `404` | Chat topilmadi |
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
