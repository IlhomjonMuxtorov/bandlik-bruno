info:
  name: MVD Driver License
  type: http
  seq: 2
http:
  method: GET
  url: '{{ base_url }}/mobile/candidate/mvd-driver-license'
  headers:
  - name: Accept
    value: application/json
  - name: Accept-Language
    value: uz
  auth:
    mode: bearer
    bearer: '{{ access_token }}'
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
