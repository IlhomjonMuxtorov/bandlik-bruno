info:
  name: 8c. Visa — Delete
  type: http
  seq: 15
http:
  method: DELETE
  url: '{{base_url}}/mobile/candidate/visa/1'
  headers:
  - name: Accept
    value: application/json
  - name: Accept-Language
    value: uz
  auth:
    mode: bearer
    bearer: '{{access_token}}'
docs: |
  Vizani o'chirish. `204 No Content`.

  **Auth:** `access_token`.

  ### ⚠️ Errors

  | Code | Reason |
  |---|---|
  | `404` | Viza topilmadi yoki boshqa nomzodga tegishli |
  | `401` | Token yo'q yoki muddati o'tgan |

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
