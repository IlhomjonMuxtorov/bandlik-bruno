info:
  name: 4. Languages
  type: http
  seq: 4
http:
  method: GET
  url: '{{base_url}}/mobile/languages'
  headers:
  - name: Accept
    value: application/json
  - name: Accept-Language
    value: uz
  auth:
    mode: bearer
    bearer: '{{access_token}}'
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
