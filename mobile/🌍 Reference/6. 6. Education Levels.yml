info:
  name: 6. Education Levels
  type: http
  seq: 6
http:
  method: GET
  url: '{{ base_url }}/mobile/education-levels'
  headers:
  - name: Accept
    value: application/json
  - name: Accept-Language
    value: uz
  auth:
    mode: bearer
    bearer: '{{ access_token }}'
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
