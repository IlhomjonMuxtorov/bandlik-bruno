info:
  name: 3. Cities List
  type: http
  seq: 3
http:
  method: GET
  url: '{{ base_url }}/mobile/cities'
  headers:
  - name: Accept
    value: application/json
  - name: Accept-Language
    value: uz
  auth:
    mode: bearer
    bearer: '{{ guest_token }}'
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
