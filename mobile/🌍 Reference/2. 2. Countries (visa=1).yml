info:
  name: 2. Countries (visa=1)
  type: http
  seq: 2
http:
  method: GET
  url: '{{base_url}}/mobile/countries'
  headers:
  - name: Accept
    value: application/json
  - name: Accept-Language
    value: uz
  auth:
    mode: bearer
    bearer: '{{ guest_token }}'
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
