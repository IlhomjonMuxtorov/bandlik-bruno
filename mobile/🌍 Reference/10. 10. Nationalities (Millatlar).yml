info:
  name: 10. Nationalities (Millatlar)
  type: http
  seq: 10
http:
  method: GET
  url: '{{base_url}}/mobile/nationalities'
  headers:
  - name: Accept
    value: application/json
  - name: Accept-Language
    value: uz
  auth:
    mode: bearer
    bearer: '{{ access_token }}'
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
