info:
  name: 7c. Language — Delete
  type: http
  seq: 12
http:
  method: DELETE
  url: '{{base_url}}/mobile/cv/{{cv_id}}/language/1'
  headers:
  - name: Accept
    value: application/json
  - name: Accept-Language
    value: uz
  auth:
    mode: bearer
    bearer: '{{access_token}}'
docs: |
  Tilni o'chirish. `204 No Content`.

  **Auth:** `access_token`.

docs: |
  Tilni o'chirish. `204 No Content`.

  **Auth:** `access_token`.

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
