info:
  name: 10b. Course — Update
  type: http
  seq: 22
http:
  method: PATCH
  url: '{{base_url}}/mobile/cv/{{ cv_id }}/course/1'
  headers:
  - name: Accept
    value: application/json
  - name: Accept-Language
    value: uz
  - name: Content-Type
    value: application/json
  body:
    type: json
    data: "{\n    \"course_name\": \"Kompyuter bilimlari kursi (yangilangan)\",\n    \"institution_name\": \"Najot Ta'lim\",\n    \"start_date\": \"2023-03-01\"\n}"
  auth:
    mode: bearer
    bearer: '{{ access_token }}'
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
