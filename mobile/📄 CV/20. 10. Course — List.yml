info:
  name: 10. Course — List
  type: http
  seq: 20
http:
  method: GET
  url: '{{ base_url }}/mobile/cv/{{ cv_id }}/course'
  headers:
  - name: Accept
    value: application/json
  - name: Accept-Language
    value: uz
  auth:
    mode: bearer
    bearer: '{{ access_token }}'
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
