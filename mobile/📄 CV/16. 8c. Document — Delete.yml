info:
  name: 8c. Document — Delete
  type: http
  seq: 16
http:
  method: DELETE
  url: '{{base_url}}/mobile/cv/{{cv_id}}/document/1'
  headers:
  - name: Accept
    value: application/json
  - name: Accept-Language
    value: uz
  auth:
    mode: bearer
    bearer: '{{access_token}}'
docs: |
  Hujjatni o'chirish. `204 No Content`.

  **Auth:** `access_token`.

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
