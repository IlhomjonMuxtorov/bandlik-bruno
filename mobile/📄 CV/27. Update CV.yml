info:
  name: Update CV
  type: http
  seq: 27
http:
  method: PATCH
  url: '{{base_url}}/mobile/cv/{{cv_id}}'
  headers:
  - name: Accept
    value: application/json
  - name: Accept-Language
    value: uz
  - name: Content-Type
    value: application/json
  body:
    type: json
    data: "{\n    \"title\": \"Haydovchi CVim — yangilangan\",\n    \"target_profession\": \"International truck driver\"\n}"
  auth:
    mode: bearer
    bearer: '{{access_token}}'
docs: |
  CV meta ma'lumotlarini yangilash (title, summary, target, photo, is_primary).

  **Auth:** `access_token`.

  ### 📥 Request Body

  | Field | Type | Required | Description |
  |---|---|:---:|---|
  | `title` | string | ✅ | CV nomi |
  | `summary` | string \| null | ➖ | Tavsif |
  | `target_profession` | string \| null | ➖ | Maqsadli ixtisos |
  | `target_country_id` | int \| null | ➖ | `country.id` |
  | `photo_url` | string \| null | ➖ | CV rasm override |
  | `is_primary` | bool \| null | ➖ | `true` bo'lsa boshqa CV-larda `is_primary` avtomatik `false` bo'ladi |

  ### 📤 Response

  To'liq CV (`/cv/{id}` formatida).

  ### ⚠️ Errors

  | Code | Reason |
  |---|---|
  | `400` | Validatsiya xatosi |
  | `404` | CV topilmadi |

docs: |
  CV meta ma'lumotlarini yangilash (title, summary, target, photo, is_primary).

  **Auth:** `access_token`.

  ### 📥 Request Body

  | Field | Type | Required | Description |
  |---|---|:---:|---|
  | `title` | string | ✅ | CV nomi |
  | `summary` | string \| null | ➖ | Tavsif |
  | `target_profession` | string \| null | ➖ | Maqsadli ixtisos |
  | `target_country_id` | int \| null | ➖ | `country.id` |
  | `photo_url` | string \| null | ➖ | CV rasm override |
  | `is_primary` | bool \| null | ➖ | `true` bo'lsa boshqa CV-larda `is_primary` avtomatik `false` bo'ladi |

  ### 📤 Response

  To'liq CV (`/cv/{id}` formatida).

  ### ⚠️ Errors

  | Code | Reason |
  |---|---|
  | `400` | Validatsiya xatosi |
  | `404` | CV topilmadi |

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
