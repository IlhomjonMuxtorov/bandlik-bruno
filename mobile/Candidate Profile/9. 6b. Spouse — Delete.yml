info:
  name: 6b. Spouse — Delete
  type: http
  seq: 9
http:
  method: DELETE
  url: '{{base_url}}/mobile/candidate/spouse'
  headers:
  - name: Accept
    value: application/json
  auth: inherit
docs: 'Turmush o''rtog''i yozuvini o''chirish.


  > `marital_status` alohida `cv/profile` orqali update qilinadi.


  > ZAGS dan olib kelingan yozuvni (`source="mvd_zags"`) o''chirib bo''lmaydi —
  > `403 Forbidden` qaytadi.


  **Auth:** `access_token`.


  ### ⚠️ Errors


  | Code | Reason |

  |---|---|

  | `403` | Yozuv ZAGS dan olib kelingan — o''chirib bo''lmaydi |

  '
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
