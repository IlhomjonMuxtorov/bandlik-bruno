info:
  name: 5. Family — List
  type: http
  seq: 3
http:
  method: GET
  url: '{{base_url}}/mobile/candidate/family-member'
  headers:
  - name: Accept
    value: application/json
  - name: Accept-Language
    value: uz
  auth: inherit
docs: 'Oila a''zolari ro''yxati (ota, ona, aka, opa).


  **Auth:** `access_token`.


  ### 📤 Response *(massiv elementi)*


  | Field | Type | Description |

  |---|---|---|

  | `id` | int | Yozuv ID |

  | `relationship` | string | `father` \| `mother` \| `brother` \| `sister` |

  | `full_name` | string | To''liq ism |

  | `birth_date` | date | `YYYY-MM-DD` |

  '
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
