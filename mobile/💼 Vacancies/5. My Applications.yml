info:
  name: My Applications
  type: http
  seq: 5
http:
  method: GET
  url: '{{base_url}}/mobile/my-applications'
  auth:
    mode: bearer
    bearer: '{{ access_token }}'
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
