info:
  name: Apply to Vacancy
  type: http
  seq: 4
http:
  method: POST
  url: '{{base_url}}/mobile/vacancies/1/apply'
  headers:
  - name: Content-Type
    value: application/json
  body:
    type: json
    data: "{\n  \"cv_id\": null,\n  \"cover_message\": \"Men bu ish o'rni uchun motivatsiyalanganman\",\n  \"hotel_preference\": true\n}"
  auth:
    mode: bearer
    bearer: '{{access_token}}'
docs: |
  This endpoint handles POST request.
  
  ### 📥 Request Body
  
  | Field | Type | Required | Description |
  |---|---|:---:|---|
  | `cv_id` | NULL | ✅ | — |
  | `cover_message` | string | ✅ | — |
  | `hotel_preference` | boolean | ✅ | — |
  
  ### 📤 Response
  
  | Field | Type | Description |
  |---|---|---|
  | `success` | boolean | Request success status |
  | `data` | object | Response data |
  
  ### ⚠️ Errors
  
  | Code | Reason |
  |---|---|
  | `400` | Invalid request data |
  | `401` | Unauthorized |
  | `404` | Not found |
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
