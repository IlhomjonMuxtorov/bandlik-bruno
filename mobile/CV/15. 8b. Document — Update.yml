info:
  name: 8b. Document — Update
  type: http
  seq: 15
http:
  method: PATCH
  url: '{{base_url}}/mobile/cv/{{cv_id}}/document/1'
  headers:
  - name: Accept
    value: application/json
  - name: Content-Type
    value: application/json
  body:
    type: json
    data: "{\n    \"type\": \"passport_scan\",\n    \"title\": \"Passport_renamed.pdf\"\
      ,\n    \"file_id\": 42\n}"
  auth: inherit
docs: 'Hujjatni yangilash. Maydonlar `create` bilan bir xil. Fayl: `file_id`.


  **Auth:** `access_token`.


  ### 📥 Request Body


  | Field | Type | Required | Description |

  |---|---|:---:|---|

  | `type` | string | ✅ | `passport_scan` \| `foreign_passport_scan` \| `criminal_record_scan`
  \| `photo_3x4_scan` |

  | `title` | string | ✅ | Fayl nomi |

  | `file_id` | int | ➖ | Yangi `upload_file_id`. Eski fayl avtomatik o''chiriladi. |


  ### 📎 `type` qiymatlari


  - `passport_scan` — Ichki pasport nusxasi

  - `foreign_passport_scan` — Xorijiy (biometrik) pasport nusxasi

  - `criminal_record_scan` — Sudlanmaganlik haqida guvohnoma

  - `photo_3x4_scan` — 3.5 × 4.5 rasm

  '
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
