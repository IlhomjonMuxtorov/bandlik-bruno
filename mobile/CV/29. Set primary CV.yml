info:
  name: Set primary CV
  type: http
  seq: 29
http:
  method: POST
  url: '{{base_url}}/mobile/cv/{{cv_id}}/set-primary'
  headers:
  - name: Accept
    value: application/json
  auth: inherit
docs: 'Berilgan CV-ni primary qiladi. Eski primary CV avtomatik `is_primary = false`
  bo''ladi.


  **Auth:** `access_token` — `is_verified=true` (tasdiqlangan shaxs) talab qilinadi.


  ### 📤 Response


  To''liq CV (`/cv/{id}` formatida) — `is_primary: true` bilan.


  ### ⚠️ Errors


  | Code | Reason |

  |---|---|

  | `403` | Shaxs tasdiqlanmagan (`is_verified=false`) — javobda `verification_required: true` |

  | `456` | Guest — registratsiya tugatilmagan |

  | `404` | CV topilmadi |

  '
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
