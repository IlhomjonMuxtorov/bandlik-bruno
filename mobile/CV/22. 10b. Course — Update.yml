info:
  name: 10b. Course — Update
  type: http
  seq: 22
http:
  method: PATCH
  url: '{{base_url}}/mobile/cv/{{cv_id}}/course/1'
  headers:
  - name: Accept
    value: application/json
  - name: Content-Type
    value: application/json
  body:
    type: json
    data: "{\n    \"course_name\": \"Kompyuter bilimlari kursi (yangilangan)\",\n\
      \    \"institution_name\": \"Najot Ta'lim\",\n    \"start_date\": \"01.03.2023\"\
      \n}"
  auth: inherit
docs: 'Kursni yangilash. Maydonlar `create` bilan bir xil.


  **Auth:** `access_token` — `is_verified=true` (tasdiqlangan shaxs) talab qilinadi.

  '
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
