info:
  name: 12.2c. Creative Work — Delete
  type: http
  seq: 55
http:
  method: DELETE
  url: '{{base_url}}/mobile/cv/{{cv_id}}/creative-work/{{id}}'
  headers:
  - name: Accept
    value: application/json
  auth: inherit
docs: |
  "Ijodiy ishlar" yozuvini o'chiradi. Muvaffaqiyatda `204 No Content`.

  **Auth:** `access_token` — `is_verified=true`.

  ### ⚠️ Errors

  | Code | Reason |
  |---|---|
  | `403` | Shaxs tasdiqlanmagan / yozuv boshqa CV'ga tegishli |
  | `404` | CV yoki yozuv topilmadi |
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
