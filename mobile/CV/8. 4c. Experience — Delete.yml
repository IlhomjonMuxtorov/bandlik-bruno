info:
  name: 4c. Experience — Delete
  type: http
  seq: 8
http:
  method: DELETE
  url: '{{base_url}}/mobile/cv/{{cv_id}}/experience/1'
  headers:
  - name: Accept
    value: application/json
  - name: Accept-Language
    value: uz
  auth:
    mode: bearer
    bearer: '{{access_token}}'
docs: |
  Tajribani o'chirish. `204 No Content`.

  **Auth:** `access_token`.

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
