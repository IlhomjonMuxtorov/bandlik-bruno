info:
  name: 4c. Experience — Delete
  type: http
  seq: 8
http:
  method: DELETE
  url: '{{base_url}}/mobile/cv/{{cv_id}}/experience/1'
  headers:
  - name: Accept
    value: application/json
  auth: inherit
docs: 'Tajribani o''chirish. `204 No Content`.


  **Auth:** `access_token` — `is_verified=true` (tasdiqlangan shaxs) talab qilinadi.

  '
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
