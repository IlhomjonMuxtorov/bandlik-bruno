info:
  name: 8a. Document — Create
  type: http
  seq: 14
http:
  method: POST
  url: '{{base_url}}/mobile/cv/{{cv_id}}/document'
  headers:
  - name: Accept
    value: application/json
  - name: Accept-Language
    value: uz
  - name: Content-Type
    value: application/json
  body:
    type: json
    data: "{\n    \"type\": \"passport_scan\",\n    \"title\": \"Passport.pdf\",\n\
      \    \"file_id\": 42\n}"
  auth: inherit
docs: 'Yangi hujjat qo''shish.


  **Auth:** `access_token`.


  ### 📥 Request Body


  | Field | Type | Required | Description |

  |---|---|:---:|---|

  | `type` | string | ✅ | `passport_scan` \| `foreign_passport_scan` \| `criminal_record_scan`
  \| `photo_3x4_scan` |

  | `title` | string | ✅ | Fayl nomi |

  | `file_id` | int | ✅ | `/mobile/upload` orqali yuklangan fayl ID |


  ### 📎 `type` qiymatlari


  - `passport_scan` — Ichki pasport nusxasi

  - `foreign_passport_scan` — Xorijiy (biometrik) pasport nusxasi

  - `criminal_record_scan` — Sudlanmaganlik haqida guvohnoma

  - `photo_3x4_scan` — 3.5 × 4.5 rasm

  '
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
