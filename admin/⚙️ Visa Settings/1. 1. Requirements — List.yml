info:
  name: 1. Requirements — List
  type: http
  seq: 1
http:
  method: GET
  url: '{{base_url}}/admin/visa-requirements'
  auth:
    mode: bearer
    bearer: '{{admin_token}}'
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
