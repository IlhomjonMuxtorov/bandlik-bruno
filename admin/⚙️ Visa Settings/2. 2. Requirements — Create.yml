info:
  name: 2. Requirements — Create
  type: http
  seq: 2
http:
  method: POST
  url: '{{base_url}}/admin/visa-requirements'
  headers:
  - name: Content-Type
    value: application/json
  body:
    type: json
    data: "{\n  \"name\": {\"uz\": \"Pasportni tasdiqlovchi hujjat\", \"ru\": \"Подтверждающий паспорт документ\", \"en\": \"Passport confirmation document\"},\n  \"description\": {\"uz\": \"Asli yoki sertifikatsiya nusxasi\", \"ru\": \"Оригинал или заверенная копия\", \"en\": \"Original or certified copy\"}\n}"
  auth:
    mode: bearer
    bearer: '{{admin_token}}'
docs: |
  This endpoint handles POST request.
  
  ### 📥 Request Body
  
  | Field | Type | Required | Description |
  |---|---|:---:|---|
  | `name` | object | ✅ | — |
  | `description` | object | ✅ | — |
  
  ### 📤 Response
  
  | Field | Type | Description |
  |---|---|---|
  | `success` | boolean | Request success status |
  | `data` | object | Response data |
  
  ### ⚠️ Errors
  
  | Code | Reason |
  |---|---|
  | `400` | Invalid request data |
  | `401` | Unauthorized |
  | `404` | Not found |
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
