info:
  name: 5. Benefits — List
  type: http
  seq: 5
http:
  method: GET
  url: '{{base_url}}/admin/visa-benefits'
  auth:
    mode: bearer
    bearer: '{{admin_token}}'
docs: |
  Viza imtiyozlari ro'yxati (pagination).

  **Auth:** `admin_token`.

docs: |
  Viza imtiyozlari ro'yxati (pagination).

  **Auth:** `admin_token`.

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
