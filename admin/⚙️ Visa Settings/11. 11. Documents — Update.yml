info:
  name: 11. Documents — Update
  type: http
  seq: 11
http:
  method: POST
  url: '{{base_url}}/admin/visa-documents/1'
  headers:
  - name: Content-Type
    value: application/json
  body:
    type: json
    data: "{\n  \"name\": {\"uz\": \"O'zgartirilgan hujjat\", \"ru\": \"Измененный документ\", \"en\": \"Modified document\"},\n  \"description\": null\n}"
  auth:
    mode: bearer
    bearer: '{{admin_token}}'
docs: |
  This endpoint handles POST request.
  
  ### 📥 Request Body
  
  | Field | Type | Required | Description |
  |---|---|:---:|---|
  | `name` | object | ✅ | — |
  | `description` | NULL | ✅ | — |
  
  ### 📤 Response
  
  | Field | Type | Description |
  |---|---|---|
  | `success` | boolean | Request success status |
  | `data` | object | Response data |
  
  ### ⚠️ Errors
  
  | Code | Reason |
  |---|---|
  | `400` | Invalid request data |
  | `401` | Unauthorized |
  | `404` | Not found |
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
