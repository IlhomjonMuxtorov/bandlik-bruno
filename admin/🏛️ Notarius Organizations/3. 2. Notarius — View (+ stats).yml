info:
  name: 2. Notarius — View (+ stats)
  type: http
  seq: 3
http:
  method: GET
  url: '{{base_url}}/admin/notarius/1'
  auth:
    mode: bearer
    bearer: '{{admin_token}}'
docs: |
  Bitta Notarius tashkiloti + `stats` sub-object:

  ```json
  {
    "id": 1,
    "type": 2,
    "type_label": "Notarius",
    "name": "...",
    ...
    "stats": {
      "total_revenue": 48000000,
      "clients_count": 1920,
      "services_count": 10,
      "employees_count": 12
    }
  }
  ```

  **Auth:** `admin_token`.

  ### ⚠️ Errors

  | Code | Reason |
  |---|---|
  | `404` | Notarius topilmadi (cross-type isolation: XBA/Visa id'si bilan urinilganda ham 404) |

docs: |
  Bitta Notarius tashkiloti + `stats` sub-object:

  ```json
  {
    "id": 1,
    "type": 2,
    "type_label": "Notarius",
    "name": "...",
    ...
    "stats": {
      "total_revenue": 48000000,
      "clients_count": 1920,
      "services_count": 10,
      "employees_count": 12
    }
  }
  ```

  **Auth:** `admin_token`.

  ### ⚠️ Errors

  | Code | Reason |
  |---|---|
  | `404` | Notarius topilmadi (cross-type isolation: XBA/Visa id'si bilan urinilganda ham 404) |

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
