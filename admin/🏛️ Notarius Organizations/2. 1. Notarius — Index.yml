info:
  name: 1. Notarius — Index
  type: http
  seq: 2
http:
  method: GET
  url: '{{base_url}}/admin/notarius?per-page=20'
  auth:
    mode: bearer
    bearer: '{{ admin_token }}'
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
