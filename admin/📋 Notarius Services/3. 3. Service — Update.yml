info:
  name: 3. Service — Update
  type: http
  seq: 3
http:
  method: POST
  url: '{{base_url}}/admin/notarius-services/1'
  headers:
  - name: Content-Type
    value: application/json
  body:
    type: json
    data: "{\n  \"price\": 150000000\n}"
  auth:
    mode: bearer
    bearer: '{{admin_token}}'
docs: |
  Xizmatni yangilaydi (qisman update).

  **Auth:** `admin_token`.

  ### ⚠️ Errors

  | Code | Reason |
  |---|---|
  | `404` | Service topilmadi |
  | `422` | Validatsiya xatosi |

docs: |
  Xizmatni yangilaydi (qisman update).

  **Auth:** `admin_token`.

  ### ⚠️ Errors

  | Code | Reason |
  |---|---|
  | `404` | Service topilmadi |
  | `422` | Validatsiya xatosi |

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
