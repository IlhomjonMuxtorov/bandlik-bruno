info:
  name: 5. Ish beruvchi — Toggle Status
  type: http
  seq: 6
http:
  method: PATCH
  url: '{{base_url}}/admin/employers/1/toggle-status'
  auth: inherit
docs: |
  Ish beruvchi tashkilotning `is_active` qiymatini teskariga aylantiradi. Bog'liq `User.status` ham flipped.

  **Auth:** `employerManage`. XBA faqat o'zi onboard qilgan tashkilotni; boshqasiniki `404`.

  ### ⚠️ Errors

  | Code | Reason |
  |---|---|
  | `403` | `employerManage` permission yo'q |
  | `404` | Topilmadi yoki cross-org |
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
