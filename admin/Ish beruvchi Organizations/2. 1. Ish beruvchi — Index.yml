info:
  name: 1. Ish beruvchi — Index
  type: http
  seq: 2
http:
  method: GET
  url: '{{base_url}}/admin/employers?per-page=20'
  auth: inherit
docs: 'Ish beruvchi tashkilotlari ro''yxati. Faqat `type = 2` yozuvlar qaytadi.


  **Auth:** `admin_token`.


  ### 🔎 Query Parameters


  XBA Index bilan bir xil: `q`, `region_id`, `district_id`, `is_active`, `page`, `per-page`.


  ### 📤 Response


  `data.items[]` — `OrganizationOutputDTO` (`type = 2`, `type_label = "Ish beruvchi"`).

  '
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
