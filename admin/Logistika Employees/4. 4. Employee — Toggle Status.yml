info:
  name: 4. Employee — Toggle Status
  type: http
  seq: 4
http:
  method: PATCH
  url: '{{base_url}}/admin/logistics-employees/1/toggle-status'
  auth: inherit
docs: 'Xodimning `is_active` qiymatini teskariga aylantiradi.


  **Auth:** `admin_token`.

  '
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
