info:
  name: 5. Logistika — Toggle Status
  type: http
  seq: 6
http:
  method: PATCH
  url: '{{base_url}}/admin/logistics/1/toggle-status'
  auth: inherit
docs: 'Logistika tashkilotning `is_active` qiymatini teskariga aylantiradi. Bog''liq
  `User.status` ham flipped.


  **Auth:** `admin_token`. Ruxsat: `logisticsManage` (faqat superadmin/admin).

  '
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
