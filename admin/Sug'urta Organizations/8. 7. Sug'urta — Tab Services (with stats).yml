info:
  name: '7. Sug''urta — Tab: Services (with stats)'
  type: http
  seq: 8
http:
  method: GET
  url: '{{base_url}}/admin/insurance/1/services?per-page=20'
  auth: inherit
docs: 'Sug''urta tashkilotning **Xizmatlari** tabi (rasmda ko''rinadigan jadval).


  Har bir xizmat uchun per-service aggregate stats — `jarayonda / bajarilgan / bekor
  qilingan / daromad` (UZS) — bitta query''da hisoblanadi (no N+1).


  **Auth:** `admin_token`. Ruxsat: `partnerOrgView` (XBA: xba_admin/xba_manager/xba_viewer) yoki superadmin/admin.


  ### 🔎 Query Parameters


  | Param | Type | Required | Description |

  |---|---|:---:|---|

  | `q` | string | ➖ | Xizmat nomi / izoh bo''yicha qidiruv |

  | `is_active` | int (0\|1) | ➖ | |

  | `page`, `per-page` | int | ➖ | Pagination |


  ### 📤 Response (`data.items[]` — `OrganizationServiceOutputDTO`)


  | Field | Type | Description |

  |---|---|---|

  | `id` | int | |

  | `organization_id` | int | |

  | `name` | string | "Hujjatlarni notarial tasdiqlash" |

  | `description` | string \| null | |

  | `price` | int | UZS so''mda |

  | `is_active` | bool | |

  | `sort_order` | int | |

  | `in_progress_count` | int | Status = `1`. `partnerOrgView`-gina bo`lgan XBA uchun `0`. |

  | `completed_count` | int | Status = `2`. `partnerOrgView`-gina bo`lgan XBA uchun `0`. |

  | `cancelled_count` | int | Status = `3`. `partnerOrgView`-gina bo`lgan XBA uchun `0`. |

  | `revenue` | int | Yakunlangan buyurtmalar summasi (UZS). Faqat har-tur `View` ruxsati (superadmin/admin) borlarga haqiqiy; `partnerOrgView`-gina bo`lgan XBA uchun `0` (daromad yashirin). |

  | `created_at` | int | |

  | `created_at_formatted` | string | `d.m.Y H:i` (Asia/Tashkent) |

  | `updated_at` | int | |

  | `updated_at_formatted` | string | `d.m.Y H:i` (Asia/Tashkent) |

  '
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
