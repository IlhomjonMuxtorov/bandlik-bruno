info:
  name: 5. Sug'urta — Toggle Status
  type: http
  seq: 6
http:
  method: PATCH
  url: '{{base_url}}/admin/insurance/1/toggle-status'
  auth: inherit
docs: 'Sug''urta tashkilotning `is_active` qiymatini teskariga aylantiradi. Bog''liq
  `User.status` ham flipped.


  **Auth:** `admin_token`. Ruxsat: `insuranceManage` (faqat superadmin/admin).

  '
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
