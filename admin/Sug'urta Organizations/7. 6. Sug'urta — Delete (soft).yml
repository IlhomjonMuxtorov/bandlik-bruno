info:
  name: 6. Sug'urta — Delete (soft)
  type: http
  seq: 7
http:
  method: DELETE
  url: '{{base_url}}/admin/insurance/1'
  auth: inherit
docs: '**Soft delete** — `is_active = false`, bog''liq `User.status = STATUS_INACTIVE`.


  **Auth:** `admin_token`. Ruxsat: `insuranceManage` (faqat superadmin/admin).

  '
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
