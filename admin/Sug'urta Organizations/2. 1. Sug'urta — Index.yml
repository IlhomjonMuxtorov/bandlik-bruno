info:
  name: 1. Sug'urta — Index
  type: http
  seq: 2
http:
  method: GET
  url: '{{base_url}}/admin/insurance?per-page=20'
  auth: inherit
docs: 'Sug''urta tashkilotlari ro''yxati. Faqat `type = 2` yozuvlar qaytadi.


  **Auth:** `admin_token`.


  ### 🔎 Query Parameters


  XBA Index bilan bir xil: `q`, `region_id`, `district_id`, `is_active`, `page`, `per-page`.


  ### 📤 Response


  `data.items[]` — `OrganizationOutputDTO` (`type = 2`, `type_label = "Sug''urta"`).

  '
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
