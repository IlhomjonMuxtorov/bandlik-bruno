info:
  name: 5. Notarius — Toggle Status
  type: http
  seq: 6
http:
  method: PATCH
  url: '{{base_url}}/admin/notarius/1/toggle-status'
  auth: inherit
docs: 'Notarius tashkilotning `is_active` qiymatini teskariga aylantiradi. Bog''liq
  `User.status` ham flipped.


  **Auth:** `admin_token`. Ruxsat: `notariusManage` (faqat superadmin/admin).

  '
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
