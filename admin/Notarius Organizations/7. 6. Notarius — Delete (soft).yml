info:
  name: 6. Notarius — Delete (soft)
  type: http
  seq: 7
http:
  method: DELETE
  url: '{{base_url}}/admin/notarius/1'
  auth:
    mode: bearer
    bearer: '{{admin_token}}'
docs: |
  **Soft delete** — `is_active = false`, bog'liq `User.status = STATUS_INACTIVE`.

  **Auth:** `admin_token`.

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
