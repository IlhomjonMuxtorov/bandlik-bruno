info:
  name: 2. XBA — View
  type: http
  seq: 3
http:
  method: GET
  url: '{{base_url}}/admin/xba/1'
  auth: inherit
docs: 'Bitta XBA tashkiloti haqida to''liq ma''lumot + `stats` (`total_revenue`, `clients_count`,
  `services_count`, `employees_count`).


  > **Ko''p tillilik:** `name`, `description`, `address` xom `{uz, ru, en}` obyekt sifatida,
  qo''shimcha `name_localized` / `description_localized` / `address_localized` esa joriy til
  (`Accept-Language`, fallback `uz`) uchun string bilan qaytadi.


  > Cross-type isolation: agar `id` boshqa partner turidagi yozuvga tegishli bo''lsa
  — `404`.


  **Auth:** `admin_token`.


  ### ⚠️ Errors


  | Code | Reason |

  |---|---|

  | `404` | XBA topilmadi |

  '
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
