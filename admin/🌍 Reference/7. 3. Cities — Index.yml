info:
  name: 3. Cities — Index
  type: http
  seq: 7
http:
  method: GET
  url: '{{base_url}}/admin/cities?country_id=1'
  auth:
    mode: bearer
    bearer: '{{ admin_token }}'
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
