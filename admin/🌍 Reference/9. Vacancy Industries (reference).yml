info:
  name: Vacancy Industries (reference)
  type: http
  seq: 9
http:
  method: GET
  url: '{{base_url}}/admin/vacancy-industries'
  headers:
  - name: Accept-Language
    value: uz
  auth:
    mode: bearer
    bearer: '{{ admin_token }}'
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
