info:
  name: 5. Countries — Delete
  type: http
  seq: 5
http:
  method: DELETE
  url: '{{base_url}}/admin/countries/1'
  auth:
    mode: bearer
    bearer: '{{admin_token}}'
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
