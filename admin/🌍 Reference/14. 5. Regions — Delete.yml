info:
  name: 14. 5. Regions — Delete
  type: http
  seq: 14
http:
  method: DELETE
  url: '{{base_url}}/admin/regions/10'
  auth:
    mode: bearer
    bearer: '{{admin_token}}'
docs: |
  Viloyatni o'chirish.

  **Auth:** `admin_token`.

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
