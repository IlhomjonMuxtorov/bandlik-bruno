info:
  name: 2. Country — View
  type: http
  seq: 2
http:
  method: GET
  url: '{{base_url}}/admin/countries/1'
  auth:
    mode: bearer
    bearer: '{{ admin_token }}'
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
