info:
  name: 1. Service — View (with stats)
  type: http
  seq: 1
http:
  method: GET
  url: '{{base_url}}/admin/xba-services/1'
  auth: inherit
docs: 'Bitta xizmat + stats. **Auth:** `admin_token`.


  ### ⚠️ Errors


  | Code | Reason |

  |---|---|

  | `404` | Service topilmadi |

  '
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
