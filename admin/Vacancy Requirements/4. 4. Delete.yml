info:
  name: Requirement — Delete
  type: http
  seq: 4
http:
  method: DELETE
  url: '{{base_url}}/admin/vacancy-requirement/delete?id=1'
  auth: inherit
docs: |
  Delete a vacancy-requirement.
  
  **Auth:** `admin_token`