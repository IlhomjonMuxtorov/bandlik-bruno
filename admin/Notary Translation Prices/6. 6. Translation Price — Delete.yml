info:
  name: 6. Translation Price — Delete
  type: http
  seq: 6
http:
  method: DELETE
  url: '{{base_url}}/admin/notary-translation-prices/1'
  auth: inherit
docs: '**Hard delete**. Mavjud `vacancy_service.notary_translation_price_id`
  havolasi `SET NULL` bo''ladi (vakansiya snapshoti saqlanadi).


  **Auth:** `admin_token`.

  '
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
