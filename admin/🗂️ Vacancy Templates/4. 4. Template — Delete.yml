info:
  name: 4. Template — Delete
  type: http
  seq: 4
http:
  method: DELETE
  url: '{{base_url}}/admin/vacancy-templates/1'
  auth:
    mode: bearer
    bearer: '{{admin_token}}'
docs: |
  This endpoint handles DELETE request.
  
  ### 📤 Response
  
  | Field | Type | Description |
  |---|---|---|
  | `success` | boolean | Request success status |
  | `data` | object | Response data |
  
  ### ⚠️ Errors
  
  | Code | Reason |
  |---|---|
  | `400` | Invalid request data |
  | `401` | Unauthorized |
  | `404` | Not found |
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
