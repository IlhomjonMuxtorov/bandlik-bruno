info:
  name: 4. Template — Delete
  type: http
  seq: 4
http:
  method: DELETE
  url: '{{ base_url }}/admin/vacancy-templates/1'
  auth:
    mode: bearer
    bearer: '{{ admin_token }}'
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
