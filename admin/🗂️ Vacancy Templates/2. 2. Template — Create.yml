info:
  name: 2. Template — Create
  type: http
  seq: 2
http:
  method: POST
  url: '{{base_url}}/admin/vacancy-templates'
  headers:
  - name: Content-Type
    value: application/json
  body:
    type: json
    data: "{\n  \"organization_id\": 1,\n  \"name\": \"Restoran Xizmatchisi (Polsha)\",\n  \"description\": \"Standart shablon Polsha bo'yicha\",\n  \"payload\": {\n    \"industry_id\": 1,\n    \"title\": \"Restoran Xizmatchisi\",\n    \"employment_type\": \"full_time\",\n    \"hiring_quota\": 12,\n    \"country_id\": 1\n  }\n}"
  auth:
    mode: bearer
    bearer: '{{admin_token}}'
docs: |
  This endpoint handles POST request.
  
  ### 📥 Request Body
  
  | Field | Type | Required | Description |
  |---|---|:---:|---|
  | `organization_id` | int | ✅ | — |
  | `name` | string | ✅ | — |
  | `description` | string | ✅ | — |
  | `payload` | object | ✅ | — |
  
  ### 📤 Response
  
  | Field | Type | Description |
  |---|---|---|
  | `success` | boolean | Request success status |
  | `data` | object | Response data |
  
  ### ⚠️ Errors
  
  | Code | Reason |
  |---|---|
  | `400` | Invalid request data |
  | `401` | Unauthorized |
  | `404` | Not found |
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
