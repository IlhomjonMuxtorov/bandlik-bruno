info:
  name: 3. Template — Update
  type: http
  seq: 3
http:
  method: PUT
  url: '{{ base_url }}/admin/vacancy-templates/1'
  headers:
  - name: Content-Type
    value: application/json
  body:
    type: json
    data: "{\n  \"organization_id\": 1,\n  \"name\": \"Restoran Xizmatchisi (Polsha) — v2\",\n  \"payload\": {},\n  \"is_active\": true\n}"
  auth:
    mode: bearer
    bearer: '{{ admin_token }}'
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
