info:
  name: 3. Template — Update
  type: http
  seq: 3
http:
  method: PUT
  url: '{{base_url}}/admin/vacancy-templates/1'
  headers:
  - name: Content-Type
    value: application/json
  body:
    type: json
    data: "{\n  \"organization_id\": 1,\n  \"name\": \"Restoran Xizmatchisi (Polsha) — v2\",\n  \"payload\": {},\n  \"is_active\": true\n}"
  auth:
    mode: bearer
    bearer: '{{admin_token}}'
docs: |
  This endpoint handles PUT request.
  
  ### 📥 Request Body
  
  | Field | Type | Required | Description |
  |---|---|:---:|---|
  | `organization_id` | int | ✅ | — |
  | `name` | string | ✅ | — |
  | `payload` | object | ✅ | — |
  | `is_active` | boolean | ✅ | — |
  
  ### 📤 Response
  
  | Field | Type | Description |
  |---|---|---|
  | `success` | boolean | Request success status |
  | `data` | object | Response data |
  
  ### ⚠️ Errors
  
  | Code | Reason |
  |---|---|
  | `400` | Invalid request data |
  | `401` | Unauthorized |
  | `404` | Not found |
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
