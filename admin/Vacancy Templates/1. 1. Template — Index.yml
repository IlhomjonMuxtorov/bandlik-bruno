info:
  name: 1. Template — Index
  type: http
  seq: 1
http:
  method: GET
  url: '{{base_url}}/admin/vacancy-templates'
  auth:
    mode: bearer
    bearer: '{{admin_token}}'
docs: |
  Shablonlar ro'yxati. Admin barchasini, org foydalanuvchi faqat o'zinikini.

  **Auth:** Admin yoki org foydalanuvchi.

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
