info:
  name: 4. Pipeline — Drop Out
  type: http
  seq: 4
http:
  method: POST
  url: '{{base_url}}/admin/vacancy-candidates/1/drop-out'
  headers:
  - name: Content-Type
    value: application/json
  body:
    type: json
    data: "{\n  \"reason\": \"Nomzod muddatni o'tkazdi\"\n}"
  auth: inherit
docs: 'This endpoint handles POST request.


  ### 📥 Request Body


  | Field | Type | Required | Description |

  |---|---|:---:|---|

  | `reason` | string | ✅ | — |


  ### 📤 Response


  | Field | Type | Description |

  |---|---|---|

  | `success` | boolean | Request success status |

  | `data` | object | Yangilangan pipeline yozuvi — `Pipeline — View` bilan bir xil shakl (`stages[]` + `service_groups[]`) |


  ### ⚠️ Errors


  | Code | Reason |

  |---|---|

  | `400` | Invalid request data |

  | `401` | Unauthorized |

  | `404` | Not found |

  '
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
