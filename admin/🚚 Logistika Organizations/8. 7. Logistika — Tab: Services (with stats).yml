info:
  name: '7. Logistika — Tab: Services (with stats)'
  type: http
  seq: 8
http:
  method: GET
  url: '{{base_url}}/admin/logistics/1/services?per-page=20'
  auth:
    mode: bearer
    bearer: '{{admin_token}}'
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
