info:
  name: 4. Logistika — Update
  type: http
  seq: 5
http:
  method: POST
  url: '{{ base_url }}/admin/logistics/1'
  body:
    type: multipartForm
    data:
    - name: phone
      value: '+998944452399'
  auth:
    mode: bearer
    bearer: '{{ admin_token }}'
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
