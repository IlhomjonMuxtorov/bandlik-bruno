info:
  name: 1. Service — View (with stats)
  type: http
  seq: 1
http:
  method: GET
  url: '{{base_url}}/admin/insurance-services/1'
  auth:
    mode: bearer
    bearer: '{{admin_token}}'
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
