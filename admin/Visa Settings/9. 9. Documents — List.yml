info:
  name: 9. Documents — List
  type: http
  seq: 9
http:
  method: GET
  url: '{{base_url}}/admin/visa-documents'
  auth:
    mode: bearer
    bearer: '{{admin_token}}'
docs: |
  Viza uchun zarur hujjatlar ro'yxati (pagination).

  **Auth:** `admin_token`.

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
