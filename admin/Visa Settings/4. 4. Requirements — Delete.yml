info:
  name: 4. Requirements — Delete
  type: http
  seq: 4
http:
  method: DELETE
  url: '{{base_url}}/admin/visa-requirements/1'
  auth: inherit
docs: 'Viza talabini o''chiradi.


  **Auth:** `admin_token`.

  '
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
