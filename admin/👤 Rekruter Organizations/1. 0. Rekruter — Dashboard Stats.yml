info:
  name: 0. Rekruter — Dashboard Stats
  type: http
  seq: 1
http:
  method: GET
  url: '{{ base_url }}/admin/recruiters/stats'
  auth:
    mode: bearer
    bearer: '{{ admin_token }}'
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
