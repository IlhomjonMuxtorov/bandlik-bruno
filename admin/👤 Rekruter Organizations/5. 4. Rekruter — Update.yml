info:
  name: 4. Rekruter — Update
  type: http
  seq: 5
http:
  method: POST
  url: '{{base_url}}/admin/recruiters/1'
  body:
    type: multipartForm
    data:
    - name: phone
      value: '+998944452399'
  auth:
    mode: bearer
    bearer: '{{admin_token}}'
docs: |
  Mavjud Rekruter tashkilotini yangilaydi. `multipart/form-data` — faqat o'zgaradigan maydonlarni yuborish kifoya.

  **Auth:** `admin_token`.

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
