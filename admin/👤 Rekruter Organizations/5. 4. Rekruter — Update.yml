info:
  name: 4. Rekruter — Update
  type: http
  seq: 5
http:
  method: POST
  url: '{{base_url}}/admin/recruiters/1'
  headers:
  - name: Content-Type
    value: application/json
  body:
    type: json
    data: "{\n  \"phone\": \"+998944452399\"\n}"
  auth:
    mode: bearer
    bearer: '{{admin_token}}'
docs: |
  Mavjud Rekruter tashkilotini yangilaydi. `application/json` — faqat o'zgaradigan maydonlarni yuborish kifoya.

  **Auth:** `admin_token`.

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
