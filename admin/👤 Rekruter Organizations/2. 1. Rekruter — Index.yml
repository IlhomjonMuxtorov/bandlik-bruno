info:
  name: 1. Rekruter — Index
  type: http
  seq: 2
http:
  method: GET
  url: '{{base_url}}/admin/recruiters?per-page=20'
  auth:
    mode: bearer
    bearer: '{{admin_token}}'
docs: |
  Rekruter tashkilotlari ro'yxati. Faqat `type = 2` yozuvlar qaytadi.

  **Auth:** `admin_token`.

  ### 🔎 Query Parameters

  XBA Index bilan bir xil: `q`, `region_id`, `district_id`, `is_active`, `page`, `per-page`.

  ### 📤 Response

  `data.items[]` — `OrganizationOutputDTO` (`type = 2`, `type_label = "Rekruter"`).

docs: |
  Rekruter tashkilotlari ro'yxati. Faqat `type = 2` yozuvlar qaytadi.

  **Auth:** `admin_token`.

  ### 🔎 Query Parameters

  XBA Index bilan bir xil: `q`, `region_id`, `district_id`, `is_active`, `page`, `per-page`.

  ### 📤 Response

  `data.items[]` — `OrganizationOutputDTO` (`type = 2`, `type_label = "Rekruter"`).

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
