info:
  name: 6. Country Price — Delete
  type: http
  seq: 6
http:
  method: DELETE
  url: '{{base_url}}/admin/insurance-country-prices/1'
  auth: inherit
docs: 'Hard delete (notarius pricing pattern bilan bir xil). Mavjud vakansiyalarning
  `vacancy_service.insurance_country_price_id` FK''si `ON DELETE SET NULL` orqali
  NULL ga aylanadi — snapshot saqlanib qoladi (service_name, price, currency, insurance_coverage_amount).


  Vaqtinchalik yashirish uchun `toggle-status` ishlatiladi (hard delete o''rniga).


  **Auth:** `admin_token`.


  ### ⚠️ Errors


  | Code | Reason |

  |---|---|

  | `404` | Narx satri topilmadi |

  '
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
