info:
  name: 2. Country Price — View
  type: http
  seq: 2
http:
  method: GET
  url: '{{base_url}}/admin/insurance-country-prices/1'
  auth: inherit
docs: 'Bitta narx satrini batafsil ko''rsatadi (nested `organization`, `country`,
  `service_type` bilan).


  **Auth:** `admin_token`.


  ### 📤 Response


  `200` — `InsuranceCountryPriceOutputDTO` (Index ro''yxati bilan bir xil shakl).


  ### ⚠️ Errors


  | Code | Reason |

  |---|---|

  | `404` | Narx satri topilmadi |

  '
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
