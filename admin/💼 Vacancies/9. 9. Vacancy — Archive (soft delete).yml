info:
  name: 9. Vacancy — Archive (soft delete)
  type: http
  seq: 9
http:
  method: DELETE
  url: '{{base_url}}/admin/vacancies/1'
  auth:
    mode: bearer
    bearer: '{{ admin_token }}'
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
