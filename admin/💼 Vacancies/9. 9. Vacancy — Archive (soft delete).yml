info:
  name: 9. Vacancy — Archive (soft delete)
  type: http
  seq: 9
http:
  method: DELETE
  url: '{{base_url}}/admin/vacancies/1'
  auth:
    mode: bearer
    bearer: '{{admin_token}}'
docs: |
  Soft delete — status `archived`. Hech qaerda ko'rinmaydi, lekin ma'lumotlar saqlanib qoladi.

  **Auth:** Admin yoki vakansiya egasi.

docs: |
  Soft delete — status `archived`. Hech qaerda ko'rinmaydi, lekin ma'lumotlar saqlanib qoladi.

  **Auth:** Admin yoki vakansiya egasi.

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
