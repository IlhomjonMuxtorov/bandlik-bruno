info:
  name: 10. Vacancy — Status Log
  type: http
  seq: 10
http:
  method: GET
  url: '{{base_url}}/admin/vacancies/1/status-log'
  auth:
    mode: bearer
    bearer: '{{ admin_token }}'
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
