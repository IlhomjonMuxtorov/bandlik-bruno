info:
  name: 6. Vacancy — Approve (admin only)
  type: http
  seq: 6
http:
  method: POST
  url: '{{base_url}}/admin/vacancies/1/approve'
  auth:
    mode: bearer
    bearer: '{{admin_token}}'
docs: |
  This endpoint handles POST request.
  
  ### 📤 Response
  
  | Field | Type | Description |
  |---|---|---|
  | `success` | boolean | Request success status |
  | `data` | object | Response data |
  
  ### ⚠️ Errors
  
  | Code | Reason |
  |---|---|
  | `400` | Invalid request data |
  | `401` | Unauthorized |
  | `404` | Not found |
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
