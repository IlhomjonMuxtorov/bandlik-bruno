info:
  name: 6. Vacancy — Approve (admin only)
  type: http
  seq: 6
http:
  method: POST
  url: '{{base_url}}/admin/vacancies/1/approve'
  auth:
    mode: bearer
    bearer: '{{admin_token}}'
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
