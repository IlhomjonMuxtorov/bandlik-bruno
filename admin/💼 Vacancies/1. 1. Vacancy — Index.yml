info:
  name: 1. Vacancy — Index
  type: http
  seq: 1
http:
  method: GET
  url: '{{base_url}}/admin/vacancies'
  headers:
  - name: Accept-Language
    value: uz
  auth:
    mode: bearer
    bearer: '{{ admin_token }}'
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
