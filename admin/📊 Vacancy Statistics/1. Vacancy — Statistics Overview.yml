info:
  name: Vacancy — Statistics Overview
  type: http
  seq: 1
http:
  method: GET
  url: '{{base_url}}/admin/vacancies/1/statistics'
  auth:
    mode: bearer
    bearer: '{{admin_token}}'
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
