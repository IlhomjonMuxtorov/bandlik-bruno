info:
  name: 5. Educational Course — Toggle Status
  type: http
  seq: 5
http:
  method: PATCH
  url: '{{base_url}}/admin/educational-courses/1/toggle-status'
  auth: inherit
docs: 'Kurs ko''rinishini teskariga aylantiradi: `status` `active` (e''lon qilingan)
  ↔ `archived` (yashirilgan). Boshqa holatga (masalan `draft`) o''tkazish uchun `Update`
  orqali `status` ni to''g''ridan-to''g''ri yuboring.


  **Auth:** `admin_token`.

  '
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
