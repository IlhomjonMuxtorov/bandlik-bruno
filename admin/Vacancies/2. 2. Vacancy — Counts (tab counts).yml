info:
  name: 2. Vacancy — Counts (tab counts)
  type: http
  seq: 2
http:
  method: GET
  url: '{{base_url}}/admin/vacancies/counts'
  headers:
  - name: Accept-Language
    value: uz
  auth: inherit
docs: 'Vakansiyalar ro''yxati sahifasidagi tablar uchun statuslar bo''yicha sonlar.
  Har bir tabda ko''rsatiladigan count''ni qaytaradi (Barchasi, Tasdiq kutilayotgan,
  Faol, Qoralama, Yopilganlar va h.k.).


  Admin barcha vakansiyalarni sanaydi; Employer/Recruiter faqat o''z tashkilotiniki
  + biriktirilgan rekruterligini.


  **Auth:** `admin_token` yoki org foydalanuvchi.


  ### 🔎 Query Parameters


  Index (ro''yxat) bilan bir xil filterlar qo''llab-quvvatlanadi — count''lar
  filterlangan natija bilan sinxron bo''lishi uchun. `status` parametri bu yerda
  e''tiborga olinmaydi (har bir status alohida sanaladi).


  | Param | Type | Required | Description |

  |---|---|:---:|---|

  | `q` | string | ➖ | Sarlavha bo''yicha (ILIKE). |

  | `organization_id` | int | ➖ | Tashkilot ID (admin uchun). |

  | `recruiter_id` | int | ➖ | Rekruter tashkiloti ID. |

  | `agent_id` | int | ➖ | XBA agent tashkiloti ID bo''yicha toraytiradi. Faqat **admin** yoki
  **root XBA** aktori uchun ishlaydi; XBA agent aktori avtomatik faqat o''zining sonlarini ko''radi. |

  | `country_id` | int | ➖ | Davlat ID. |

  | `city_id` | int[] | ➖ | Shahar ID(lar). |

  | `industry_id` | int | ➖ | Soha ID. |

  | `employment_type` | string | ➖ | `full_time` \| `part_time` \| `seasonal` \| `contract`. |

  | `experience_months_min` | int | ➖ | Talab qilinadigan tajriba (oy) shu qiymatdan **kam bo'lmagan** vakansiyalar (`experience_months >=`). |

  | `experience_months_max` | int | ➖ | Talab qilinadigan tajriba (oy) shu qiymatdan **katta bo'lmagan** vakansiyalar (`experience_months <=`). |

  | `salary_min` / `salary_max` | int | ➖ | Maosh oralig''i — major unitda (so''m/dollar). |


  ### 📋 Headers


  | Header | Required | Description |

  |---|:---:|---|

  | `Authorization` | ✅ | `Bearer {token}` |

  | `Accept-Language` | ➖ | `uz` \| `ru` \| `en` |


  ### 📤 Response (`data`)


  | Field | Type | Description |

  |---|---|---|

  | `total` | int | Barcha statuslar yig''indisi |

  | `draft` | int | Qoralama |

  | `pending_approval` | int | 1-faza: agentlik vakansiyasi root XBA tasdig'ini kutmoqda (faqat `agent_organization_id != null` yozuvlar) |

  | `pending_association` | int | 2-faza: Uyushma (superadmin/admin) tasdig'ini kutmoqda — root XBA'ning o'z vakansiyasi bevosita shu yerga tushadi, agentlik vakansiyasi esa `pending_approval`dan o'tib keladi |

  | `approved` | int | Faol (tasdiqlangan) |

  | `rejected` | int | Rad etilgan |

  | `closed` | int | Yopilgan |

  | `archived` | int | Arxivlangan |


  ```json

  {
    "success": true,
    "data": {
      "total": 1490,
      "draft": 312,
      "pending_approval": 245,
      "pending_association": 112,
      "approved": 601,
      "rejected": 88,
      "closed": 198,
      "archived": 46
    }
  }

  ```


  ### ⚠️ Errors


  | Code | Reason |

  |---|---|

  | `401` | Token yo''q |

  | `403` | Org foydalanuvchisi (org_id yo''q yoki Employer/Recruiter emas) |

  '
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
