opencollection: 1.0.0
info:
  name: XBA — Admin API
bundled: false
extensions:
  bruno:
    ignore:
    - node_modules
    - .git
auth:
  type: bearer
  token: '{{admin_token}}'
variables:
  base_url: http://api.bandlik.loc
  admin_token: ''
