info:
  name: 1. Employee — View
  type: http
  seq: 1
http:
  method: GET
  url: '{{base_url}}/admin/employer-employees/1'
  auth:
    mode: bearer
    bearer: '{{admin_token}}'
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
