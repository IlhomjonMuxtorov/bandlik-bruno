info:
  name: 4. Employee — Toggle Status
  type: http
  seq: 4
http:
  method: PATCH
  url: '{{base_url}}/admin/visa-employees/1/toggle-status'
  auth:
    mode: bearer
    bearer: '{{admin_token}}'
docs: |
  This endpoint handles PATCH request.
  
  ### 📤 Response
  
  | Field | Type | Description |
  |---|---|---|
  | `success` | boolean | Request success status |
  | `data` | object | Response data |
  
  ### ⚠️ Errors
  
  | Code | Reason |
  |---|---|
  | `400` | Invalid request data |
  | `401` | Unauthorized |
  | `404` | Not found |
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
