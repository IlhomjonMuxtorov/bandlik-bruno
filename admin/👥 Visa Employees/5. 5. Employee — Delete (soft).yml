info:
  name: 5. Employee — Delete (soft)
  type: http
  seq: 5
http:
  method: DELETE
  url: '{{base_url}}/admin/visa-employees/1'
  auth:
    mode: bearer
    bearer: '{{admin_token}}'
docs: |
  **Soft delete** — `is_active = false`.

  **Auth:** `admin_token`.

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
