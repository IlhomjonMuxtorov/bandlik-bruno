info:
  name: Document — Update
  type: http
  seq: 3
http:
  method: POST
  url: '{{base_url}}/admin/vacancy-document/update?id=1'
  headers:
  - name: Content-Type
    value: application/json
  body:
    type: json
    json: |-
      {
        "name": {
          "uz": "Updated name"
        }
      }
  auth: inherit
docs: |
  Update an existing vacancy-document.
  
  **Auth:** `admin_token`