info:
  name: Document — List
  type: http
  seq: 1
http:
  method: GET
  url: '{{base_url}}/admin/vacancy-document'
  auth: inherit
docs: |
  List all vacancy-documents (dictionary items).
  
  **Auth:** `admin_token`