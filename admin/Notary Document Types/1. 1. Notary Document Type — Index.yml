info:
  name: 1. Notary Document Type — Index
  type: http
  seq: 1
http:
  method: GET
  url: '{{base_url}}/admin/notary-document-types?per-page=20'
  auth: inherit
docs: 'Notarius hujjat turlari ro''yxati (Pasport, Diplom, Tug''ilganlik haqida guvohnoma, …).
  Skrinshotdagi "Hujjat turi" dropdownini to''ldirish uchun ishlatiladi.


  > Ro''yxat `Accept-Language` bo''yicha alifbo (A-Z) tartibida qaytadi (fallback
  `uz`), agar `sort` yuborilmagan bo''lsa.


  **Auth:** `admin_token`.


  ### 🔎 Query Parameters


  | Param | Type | Required | Description |

  |---|---|:---:|---|

  | `q` | string | ➖ | `code` yoki `name->>''uz''/''ru''/''en''` bo''yicha ILIKE |

  | `is_active` | int (0\|1) | ➖ | |

  | `page`, `per-page` | int | ➖ | Pagination (`per-page` 1–300, default 20). |

  | `sort` | string | ➖ | Berilmasa — A-Z. `sort_order` — eski tahririy tartib.
  Ruxsat etilgan: `id` \| `sort_order` \| `code` \| `created_at` (har biri `-` prefiks
  bilan kamayuvchi bo''lishi mumkin). |


  ### 📤 Response (`data.items[]`)


  | Field | Type | Description |

  |---|---|---|

  | `id` | int | |

  | `code` | string | `passport`, `diploma`, … |

  | `name` | object | `{uz, ru, en}` JSONB |

  | `name_localized` | string | `Accept-Language` header bo''yicha tanlangan til |

  | `is_active` | bool | |

  | `sort_order` | int | |

  | `created_at` / `updated_at` | int | |

  | `created_at_formatted` / `updated_at_formatted` | string | `d.m.Y H:i` (Asia/Tashkent) |

  '
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
