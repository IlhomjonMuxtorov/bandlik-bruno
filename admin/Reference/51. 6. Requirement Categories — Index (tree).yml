info:
  name: 51. 6. Requirement Categories — Index (tree)
  type: http
  seq: 51
http:
  method: GET
  url: '{{base_url}}/admin/requirement-categories'
  params:
  - name: active_only
    value: '1'
    enabled: false
  headers:
  - name: Authorization
    value: 'Bearer {{admin_token}}'
  auth: inherit
docs: |-
  Talab taksonomiyasi — 8 ta yuqori guruh va ularning subgruplari daraxt ko'rinishida.

  > Daraxtning **har bir darajasi** `Accept-Language` bo'yicha alifbo (A-Z)
  > tartibida qaytadi (fallback `uz`), agar `sort` yuborilmagan bo'lsa. `?sort=sort_order`
  > eski tahririy (editorial) tartibni qaytaradi — paginatsiya yo'q, faqat A-Z/`sort_order`
  > tumblerini boshqaradi.

  **Auth:** `admin_token` (faqat `admin`).

  ### 🔎 Query Parameters

  | Param | Type | Required | Description |
  |---|---|:---:|---|
  | `active_only` | int (0\|1) | ➖ | `1` — faqat faol guruh/subgruplar. Default: hammasi. |
  | `sort` | string | ➖ | Berilmasa — A-Z (har bir daraja). `sort_order` — eski
  tahririy tartib. |

  ### 📤 Response (`data[]`)

  | Field | Type | Description |
  |---|---|---|
  | `id` | int | |
  | `parent_id` | int \| null | Yuqori guruhda `null`. |
  | `code` | string | Masalan `personal`, `industry.driver`. |
  | `name` | object | `{uz,ru,en}`. |
  | `children` | array | Subgruplar (rekursiv). |
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
