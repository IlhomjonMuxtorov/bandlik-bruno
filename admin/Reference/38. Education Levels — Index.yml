info:
  name: Education Levels — Index
  type: http
  seq: 38
http:
  method: GET
  url: '{{base_url}}/admin/education-levels'
  auth: inherit
params: {}
headers:
  - name: Accept-Language
    value: uz
    enabled: true
docs: >
  Ta'lim darajalari ro'yxati — leadlar Filter panelidagi "Ta'lim darajasi"
  dropdowni uchun.


  **Auth:** `access_token` (admin).


  ### 📤 Response (`data`)

  Massiv: `[{ id, code, name }]` (`code` masalan `high_school`/`bachelor`/`master`;
  `name` — `Accept-Language` bo'yicha string).
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
