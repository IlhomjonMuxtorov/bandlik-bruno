info:
  name: 55. 10. Requirement Categories — Delete
  type: http
  seq: 55
http:
  method: DELETE
  url: '{{base_url}}/admin/requirement-categories/1'
  headers:
  - name: Authorization
    value: 'Bearer {{admin_token}}'
  auth: inherit
docs: |-
  Talab kategoriyasini o'chiradi. Unga bog'langan definition'lar avtomatik
  ajratiladi (FK `ON DELETE SET NULL`).

  **Auth:** `admin_token` (faqat `admin`).
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
