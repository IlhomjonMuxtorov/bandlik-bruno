info:
  name: 16. 2. District — View
  type: http
  seq: 16
http:
  method: GET
  url: '{{base_url}}/admin/districts/1001'
  auth: inherit
docs: 'Bitta tuman ma''lumotlarini olish.


  **Auth:** `admin_token`.


  ### 📋 Headers


  | Header | Required | Description |

  |---|:---:|---|

  | `Authorization` | ✅ | `Bearer {{ _.admin_token }}` |

  | `Accept-Language` | ➖ | `uz` \| `ru` \| `en`. Default: `uz`. |


  ### 📤 Response


  | Field | Type | Description |

  |---|---|---|

  | `id` | int | GCP SP_ID |

  | `region_id` | int | |

  | `name` | string | Til bo''yicha tarjima |

  | `is_active` | bool | |

  | `sort_order` | int | |

  '
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
