info:
  name: 52. 7. Requirement Category — View
  type: http
  seq: 52
http:
  method: GET
  url: '{{base_url}}/admin/requirement-categories/1'
  headers:
  - name: Authorization
    value: 'Bearer {{admin_token}}'
  auth: inherit
docs: |-
  Bitta talab kategoriyasi (subgruplari bilan).

  **Auth:** `admin_token` (faqat `admin`).
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
