info:
  name: 27. 7. Cities — Delete
  type: http
  seq: 27
http:
  method: DELETE
  url: '{{base_url}}/admin/cities/1'
  auth: inherit
docs: |
  Shaharni o'chirish.

  **Auth:** `admin_token`.

  ### ⚠️ Errors

  | Code | Reason |
  |---|---|
  | `404` | Shahar topilmadi |
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
