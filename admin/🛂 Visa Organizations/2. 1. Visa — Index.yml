info:
  name: 1. Visa — Index
  type: http
  seq: 2
http:
  method: GET
  url: '{{base_url}}/admin/visa?per-page=20'
  auth:
    mode: bearer
    bearer: '{{admin_token}}'
docs: |
  This endpoint handles GET request.
  
  ### 📤 Response
  
  | Field | Type | Description |
  |---|---|---|
  | `success` | boolean | Request success status |
  | `data` | object | Response data |
  
  ### ⚠️ Errors
  
  | Code | Reason |
  |---|---|
  | `400` | Invalid request data |
  | `401` | Unauthorized |
  | `404` | Not found |
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
