info:
  name: 4. Visa — Update
  type: http
  seq: 5
http:
  method: POST
  url: '{{base_url}}/admin/visa/1'
  body:
    type: multipartForm
    data:
    - name: phone
      value: '+998901112299'
  auth:
    mode: bearer
    bearer: '{{admin_token}}'
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
