info:
  name: 2. Visa — View
  type: http
  seq: 3
http:
  method: GET
  url: '{{base_url}}/admin/visa/1'
  auth:
    mode: bearer
    bearer: '{{admin_token}}'
docs: |
  Bitta Viza tashkiloti haqida to'liq ma'lumot + `stats`.

  **Auth:** `admin_token`.

  ### ⚠️ Errors

  | Code | Reason |
  |---|---|
  | `404` | Viza tashkilot topilmadi |

docs: |
  Bitta Viza tashkiloti haqida to'liq ma'lumot + `stats`.

  **Auth:** `admin_token`.

  ### ⚠️ Errors

  | Code | Reason |
  |---|---|
  | `404` | Viza tashkilot topilmadi |

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
