info:
  name: 5. Visa — Toggle Status
  type: http
  seq: 6
http:
  method: PATCH
  url: '{{ base_url }}/admin/visa/1/toggle-status'
  auth:
    mode: bearer
    bearer: '{{ admin_token }}'
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
