info:
  name: 1. Employee — View
  type: http
  seq: 1
http:
  method: GET
  url: '{{base_url}}/admin/recruiter-employees/1'
  auth: inherit
docs: 'Bitta xodim. Javobda `country` / `city` obyektlari ham bor (`{id, name: {uz, ru, en}}`
  yoki `null`). **Auth:** `admin_token`.


  ### ⚠️ Errors


  | Code | Reason |

  |---|---|

  | `404` | Employee topilmadi |

  '
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
