info:
  name: '9. XBA — Tab: Clients (DISTINCT candidates)'
  type: http
  seq: 10
http:
  method: GET
  url: '{{base_url}}/admin/xba/1/clients?per-page=20'
  auth:
    mode: bearer
    bearer: '{{admin_token}}'
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
