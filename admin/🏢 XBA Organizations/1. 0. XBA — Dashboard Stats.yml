info:
  name: 0. XBA — Dashboard Stats
  type: http
  seq: 1
http:
  method: GET
  url: '{{base_url}}/admin/xba/stats'
  auth:
    mode: bearer
    bearer: '{{admin_token}}'
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
