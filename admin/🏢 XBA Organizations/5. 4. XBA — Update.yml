info:
  name: 4. XBA — Update
  type: http
  seq: 5
http:
  method: POST
  url: '{{base_url}}/admin/xba/1'
  body:
    type: multipartForm
    data:
    - name: phone
      value: '+998941202299'
    - name: is_active
      value: '0'
  auth:
    mode: bearer
    bearer: '{{admin_token}}'
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
