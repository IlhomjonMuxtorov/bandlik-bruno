info:
  name: 10.6c. CV Edit — Project Delete
  type: http
  seq: 55
http:
  method: DELETE
  url: '{{base_url}}/admin/vacancy-applications/1/cv/project/1'
  headers:
  - name: Content-Type
    value: application/json
  auth: inherit
docs: |
  "Loyihalar" yozuvini o'chiradi.

  **Auth:** `applicationCvManage`.

  ### ⚠️ Errors

  | Code | Reason |
  |---|---|
  | `403` | `applicationCvManage` yo'q / ariza faol emas |
  | `404` | Ariza / CV / yozuv topilmadi |
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
