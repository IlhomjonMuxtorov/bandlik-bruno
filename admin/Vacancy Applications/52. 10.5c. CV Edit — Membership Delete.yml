info:
  name: 10.5c. CV Edit — Membership Delete
  type: http
  seq: 52
http:
  method: DELETE
  url: '{{base_url}}/admin/vacancy-applications/1/cv/membership/1'
  headers:
  - name: Content-Type
    value: application/json
  auth: inherit
docs: |
  "Tarmoqlar va a'zoliklar" yozuvini o'chiradi.

  **Auth:** `applicationCvManage`.

  ### ⚠️ Errors

  | Code | Reason |
  |---|---|
  | `403` | `applicationCvManage` yo'q / ariza faol emas |
  | `404` | Ariza / CV / yozuv topilmadi |
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
