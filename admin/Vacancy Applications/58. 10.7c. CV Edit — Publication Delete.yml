info:
  name: 10.7c. CV Edit — Publication Delete
  type: http
  seq: 58
http:
  method: DELETE
  url: '{{base_url}}/admin/vacancy-applications/1/cv/publication/1'
  headers:
  - name: Content-Type
    value: application/json
  auth: inherit
docs: |
  "Nashrlar" yozuvini o'chiradi.

  **Auth:** `applicationCvManage`.

  ### ⚠️ Errors

  | Code | Reason |
  |---|---|
  | `403` | `applicationCvManage` yo'q / ariza faol emas |
  | `404` | Ariza / CV / yozuv topilmadi |
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
