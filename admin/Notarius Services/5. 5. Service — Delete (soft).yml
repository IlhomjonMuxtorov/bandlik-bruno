info:
  name: 5. Service — Delete (soft)
  type: http
  seq: 5
http:
  method: DELETE
  url: '{{base_url}}/admin/notarius-services/1'
  auth: inherit
docs: '**Soft delete** — `is_active = false`.


  **Auth:** `admin_token`.

  '
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
