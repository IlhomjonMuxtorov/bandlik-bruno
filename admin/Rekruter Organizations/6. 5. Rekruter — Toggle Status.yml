info:
  name: 5. Rekruter — Toggle Status
  type: http
  seq: 6
http:
  method: PATCH
  url: '{{base_url}}/admin/recruiters/1/toggle-status'
  auth: inherit
docs: 'Rekruter tashkilotning `is_active` qiymatini teskariga aylantiradi. Bog''liq
  `User.status` ham flipped.


  **Auth:** `admin_token`.

  '
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
