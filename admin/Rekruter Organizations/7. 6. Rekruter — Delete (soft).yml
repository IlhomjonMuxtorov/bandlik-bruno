info:
  name: 6. Rekruter — Delete (soft)
  type: http
  seq: 7
http:
  method: DELETE
  url: '{{base_url}}/admin/recruiters/1'
  auth: inherit
docs: |
  **Soft delete** — `is_active = false`, bog'liq `User.status = STATUS_INACTIVE`.

  **Auth:** `recruiterManage`. XBA faqat o'zi onboard qilgan tashkilotni o'chira oladi; boshqasiniki `404`.

  ### ⚠️ Errors

  | Code | Reason |
  |---|---|
  | `403` | `recruiterManage` permission yo'q |
  | `404` | Topilmadi yoki cross-org |
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
