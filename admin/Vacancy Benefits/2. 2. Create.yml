info:
  name: Benefit — Create
  type: http
  seq: 2
http:
  method: POST
  url: '{{base_url}}/admin/vacancy-benefit/create'
  headers:
  - name: Content-Type
    value: application/json
  body:
    type: json
    json: |-
      {
        "name": {
          "uz": "Uzbek name",
          "ru": "Russian name"
        },
        "description": {
          "uz": "Uzbek desc"
        }
      }
  auth: inherit
docs: |
  Create a new vacancy-benefit.
  
  **Auth:** `admin_token`