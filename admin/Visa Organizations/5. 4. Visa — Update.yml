info:
  name: 4. Visa — Update
  type: http
  seq: 5
http:
  method: POST
  url: '{{base_url}}/admin/visa/1'
  headers:
  - name: Content-Type
    value: application/json
  body:
    type: json
    data: "{\n  \"phone\": \"+998901112299\"\n}"
  auth: inherit
docs: 'Mavjud Viza tashkilotini yangilaydi. `application/json` — faqat o''zgartirmoqchi
  bo''lgan maydonlarni yuborish kifoya. XBA `Update` bilan bir xil semantika.


  **Auth:** `admin_token`.


  ### ⚠️ Errors


  | Code | Reason |

  |---|---|

  | `404` | Viza topilmadi |

  | `422` | Validatsiya xatosi |

  | `400` | Fayl ID topilmadi yoki `status=uploading` |

  '
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
