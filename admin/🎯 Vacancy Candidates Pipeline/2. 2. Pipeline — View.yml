info:
  name: 2. Pipeline — View
  type: http
  seq: 2
http:
  method: GET
  url: '{{base_url}}/admin/vacancy-candidates/1'
  auth:
    mode: bearer
    bearer: '{{admin_token}}'
docs: |
  Pipeline yozuvi tafsiloti.

  **Auth:** Admin yoki vakansiya egasi.

docs: |
  Pipeline yozuvi tafsiloti.

  **Auth:** Admin yoki vakansiya egasi.

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
