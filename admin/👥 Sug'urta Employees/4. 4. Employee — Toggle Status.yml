info:
  name: 4. Employee — Toggle Status
  type: http
  seq: 4
http:
  method: PATCH
  url: '{{base_url}}/admin/insurance-employees/1/toggle-status'
  auth:
    mode: bearer
    bearer: '{{admin_token}}'
docs: |
  Xodimning `is_active` qiymatini teskariga aylantiradi.

  **Auth:** `admin_token`.

docs: |
  Xodimning `is_active` qiymatini teskariga aylantiradi.

  **Auth:** `admin_token`.

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
